id: storageImportNotSupported
args:
  - name: storageId
  - name: objectStorages
    type: Binaries
description: >-
  Cannot configure storage import on storage {storageId} - this operation
  requires storage with canonical path type and on object storages
  (any of: {objectStorages}) it requires blockSize = 0.
http_code: 400
