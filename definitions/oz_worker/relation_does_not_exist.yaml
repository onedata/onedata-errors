id: relationDoesNotExist
args:
  - name: childType
    type: Atom
  - name: childId
  - name: parentType
    type: Atom
  - name: parentId
x-additional_to_json_variables:
  - name: RelationToString
    assign: |
      case {ChildType, ParentType} of
          {od_space, od_provider} -> <<"is not supported by">>;
          {_, _} -> <<"is not a member of">>
      end
description: >- 
  Bad value: {childType}:{childId} TODO {parentType}:{parentId}.
http_code: 400
