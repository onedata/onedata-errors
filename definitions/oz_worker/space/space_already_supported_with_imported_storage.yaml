id: spaceAlreadySupportedWithImportedStorage
args:
  - name: spaceId
  - name: storageId
description: >-
  Space "{spaceId}" is already supported with an imported storage backend ("{storageId}").
  A space can be supported by no more than one imported storage backend.
http_code: 409
