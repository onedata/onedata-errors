id: posix
args:
  - name: errno
    type: Atom
description: >-
  Operation failed with POSIX error: {errno}.
http_code: 400
errno: |
  -spec to_errno(t()) -> od_error:errno().
  to_errno(?POSIX(Errno)) ->
      Errno.
