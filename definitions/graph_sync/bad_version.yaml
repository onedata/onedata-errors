id: badVersion
args:
  - name: supportedVersions
    type: Binaries
    # note: those are binaries in case of cdmi but ints for graph sync
description: >-
  GraphSync error: bad protocol version; supported versions: {supportedVersions}.
http_code: 400
errno: EINVAL
